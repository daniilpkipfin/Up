<UserControl x:Class="WpfApp1.Views.BookView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp1.Views"
             mc:Ignorable="d">
    <UserControl.Resources>
        <!-- Цветовая палитра -->
        <SolidColorBrush x:Key="AccentBrush" Color="#2C3E50"/>
        <SolidColorBrush x:Key="LightBackgroundBrush" Color="#F5F5F5"/>
        <SolidColorBrush x:Key="WhiteBrush" Color="White"/>
        <SolidColorBrush x:Key="TextBrush" Color="#333333"/>
        <SolidColorBrush x:Key="SubtleTextBrush" Color="#777777"/>

        <!-- Основная кнопка (зелёная) -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#27AE60"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#219A52"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#1E8449"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Опасная кнопка (красная) -->
        <Style x:Key="DangerButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#E74C3C"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#C0392B"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#A93226"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Предупреждающая кнопка (оранжевая) -->
        <Style x:Key="WarningButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#F39C12"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E08E0B"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#C87F0A"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Плоская кнопка действия -->
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#D0D0D0"/>
            <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E8F0FE"/>
                    <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#D2E3FC"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Стили для ListView -->
        <Style TargetType="ListView">
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="Background" Value="{StaticResource WhiteBrush}"/>
            <Setter Property="Padding" Value="0"/>
        </Style>
        <Style TargetType="GridViewColumnHeader">
            <Setter Property="Background" Value="{StaticResource LightBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
        </Style>
        <Style TargetType="ListViewItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F0F4F8"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#E0E8F0"/>
                </Trigger>
                <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                    <Setter Property="Background" Value="#FAFAFA"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Стили для текстовых блоков -->
        <Style x:Key="BookTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="26"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        </Style>
        <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Margin" Value="0,15,0,10"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="BorderBrush" Value="#D0D0D0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalContentAlignment" Value="Top"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Баннер заморозки -->
                <RowDefinition Height="Auto"/>
                <!-- Основная информация -->
                <RowDefinition Height="Auto"/>
                <!-- Отзывы -->
            </Grid.RowDefinitions>

            <!-- Баннер заморозки -->
            <Border x:Name="borderFrozen" 
                    Grid.Row="0"
                    Background="#E74C3C" 
                    CornerRadius="6" 
                    Padding="12,10" 
                    Margin="0,0,0,15" 
                    Visibility="Visible">
                <TextBlock Text="⚠️ КНИГА ЗАМОРОЖЕНА" 
                           FontWeight="Bold" 
                           Foreground="White" 
                           HorizontalAlignment="Center" 
                           FontSize="14"/>
            </Border>

            <!-- Основная информация: обложка + данные -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Обложка книги -->
                <Image Source="{Binding Book.CoverPath}" 
                       Width="200" Height="280" 
                       HorizontalAlignment="Left" 
                       VerticalAlignment="Top" 
                       Margin="0,0,20,0"
                       Stretch="Uniform"/>

                <!-- Правая колонка с метаданными, текстом и кнопками -->
                <StackPanel Grid.Column="1">
                    <TextBlock Text="{Binding Book.Title}" Style="{StaticResource BookTitleStyle}"/>
                    <TextBlock Text="{Binding AuthorName}" 
                               FontSize="18" 
                               Foreground="{StaticResource SubtleTextBrush}" 
                               Margin="0,0,0,5"/>
                    <TextBlock Text="{Binding GenresString}" 
                               FontStyle="Italic" 
                               Foreground="{StaticResource SubtleTextBrush}" 
                               Margin="0,0,0,15"/>

                    <!-- Описание -->
                    <TextBlock Text="Описание" 
                               FontWeight="Bold" FontSize="16" 
                               Margin="0,10,0,5" 
                               Foreground="{StaticResource AccentBrush}"/>
                    <TextBlock Text="{Binding Book.Description}" 
                               TextWrapping="Wrap" 
                               Margin="0,0,0,15" 
                               Foreground="{StaticResource TextBrush}"/>

                    <!-- Текст книги -->
                    <TextBlock Text="Текст книги" 
                               FontWeight="Bold" FontSize="16" 
                               Margin="0,10,0,5" 
                               Foreground="{StaticResource AccentBrush}"/>
                    <TextBox Text="{Binding Book.Content}" 
                             TextWrapping="Wrap" 
                             IsReadOnly="True" 
                             Height="350" 
                             VerticalScrollBarVisibility="Visible" 
                             Margin="0,0,0,20" 
                             Background="{StaticResource LightBackgroundBrush}"/>

                    <!-- Основные кнопки -->
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <Button Content="Добавить в список чтения" 
                                Style="{StaticResource PrimaryButtonStyle}" 
                                Click="AddToList_Click" 
                                Margin="0,0,10,0"/>
                        <Button Content="Оставить отзыв" 
                                Style="{StaticResource ActionButtonStyle}" 
                                Click="LeaveReview_Click" 
                                Margin="0,0,10,0"/>
                        <Button Content="Пожаловаться на книгу" 
                                Style="{StaticResource ActionButtonStyle}" 
                                Click="ComplainBook_Click" 
                                Margin="0,0,10,0"/>
                        <Button Content="Пожаловаться на автора" 
                                Style="{StaticResource WarningButtonStyle}" 
                                Click="ComplainAuthor_Click"/>
                    </StackPanel>

                    <!-- Админ панель -->
                    <StackPanel x:Name="AdminPanel" 
                                Orientation="Horizontal" 
                                Visibility="Visible">
                        <Button Content="Заморозить / Разморозить книгу" 
                                Style="{StaticResource DangerButtonStyle}"
                                Click="FreezeBook_Click"/>
                    </StackPanel>
                </StackPanel>
            </Grid>

            <!-- Отзывы читателей -->
            <StackPanel Grid.Row="2" Margin="0,10,0,0">
                <TextBlock Text="Отзывы читателей" 
                           Style="{StaticResource SectionTitleStyle}"/>
                <ListView ItemsSource="{Binding Reviews}" 
                          Height="320" 
                          AlternationCount="2">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Пользователь" 
                                            DisplayMemberBinding="{Binding Users.DisplayName}" 
                                            Width="150"/>
                            <GridViewColumn Header="Оценка" 
                                            DisplayMemberBinding="{Binding Rating}" 
                                            Width="80"/>
                            <GridViewColumn Header="Отзыв" 
                                            DisplayMemberBinding="{Binding ReviewText}" 
                                            Width="400"/>
                            <GridViewColumn Header="Статус" Width="100">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock FontWeight="SemiBold"
                                                   VerticalAlignment="Center">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Text" Value="Активен"/>
                                                    <Setter Property="Foreground" Value="#2E7D32"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsFrozen}" Value="True">
                                                            <Setter Property="Text" Value="Заморожен"/>
                                                            <Setter Property="Foreground" Value="#C62828"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Действия" Width="230">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="Пожаловаться" 
                                                    Style="{StaticResource ActionButtonStyle}"
                                                    Click="ComplainReview_Click" 
                                                    Tag="{Binding}"/>
                                            <Button Content="Заморозить"
                                                    Style="{StaticResource DangerButtonStyle}"
                                                    Click="FreezeReview_Click"
                                                    Loaded="FreezeReviewButton_Loaded"
                                                    Tag="{Binding}"
                                                    Padding="6,3"
                                                    Visibility="Collapsed"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>